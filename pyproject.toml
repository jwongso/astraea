[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "astraea-framework"
version = "0.2.0"
description = "Open-justice RAG framework - jurisdiction-specific legal Q&A over public court decisions"
readme = "README.md"
license = "MIT"
license-files = ["LICENSE"]
requires-python = ">=3.11"
authors = [{ email = "jsaputra@riseup.net" }]
keywords = ["legal", "rag", "llm", "tenancy", "jurisdiction"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
]

dependencies = [
    "fastapi>=0.111,<1.0",
    "uvicorn[standard]>=0.29,<1.0",
    "qdrant-client>=1.9,<2.0",
    "sentence-transformers>=3.0,<6.0",
    "transformers>=4.40,<6.0",
    "einops>=0.7",
    "playwright>=1.40",
    "pydantic>=2.7,<3.0",
    "httpx>=0.27,<1.0",
    "redis>=5.0,<6.0",
    "cachetools>=5.3,<6.0",
]

[project.urls]
Homepage = "https://github.com/jwongso/astraea"
Issues = "https://github.com/jwongso/astraea/issues"

[project.optional-dependencies]
dev = [
    "pytest>=8",
    "pytest-asyncio>=0.23",
    "ruff>=0.4",
]
ingest = [
    "beautifulsoup4>=4.12",
    "pypdf>=4.0",
    "pytesseract>=0.3",
    "pillow>=10.0",
]

[tool.setuptools.packages.find]
include = ["core*", "jurisdictions*", "ingest*", "examples*"]

[tool.setuptools.package-data]
core = ["frontend/*"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.pytest.ini_options]
asyncio_mode = "auto"
pythonpath = ["."]
markers = [
    "routing: Tier 0 - pure keyword routing, no Qdrant or LLM required",
    "retrieval: Tier 1 - Qdrant only, no LLM required",
    "structural: Tier 2 - LLM required, non-semantic checks",
    "llm: Tier 3 - LLM required, semantic answer quality",
]
